// Browser-safe configuration only. Never place provider secrets in this file.

const CONFIG = {
    // 공개 클라이언트에는 URL과 webhook 주소만 둘 수 있습니다. 비밀 키는 서버 환경변수로만 관리합니다.
    SAFETY_REVIEW_URL: '',
    RECOMMENDATION_URL: ''
};
