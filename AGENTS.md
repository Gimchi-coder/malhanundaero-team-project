# AI Agent Coding Instructions — 바이브코딩 캠프 팀 프로젝트 규칙

## 0. 작업 시작 전 필수 — 강의 자료 학습

- 어떤 기능이든 만들기 전에 프로젝트 폴더 안 `vibe_coding_lecture/` 디렉토리의 자료를 먼저 읽고 숙지한다.
- 학습 우선순위는 `team_prd.md` > 각 차시 `*_실습.ipynb` > 각 차시 `*_강의자료.pdf` 순서로 한다.
- 특히 다음 차시는 기능 작업 전에 재확인한다.
  - 3차시: PRD 5요소·MVP 기획
  - 4차시: Supabase 연동·배포
  - 8차시: 스코핑·평가 케이스
  - 9~13차시: 도구/MCP·RAG·멀티에이전트·계획-반성 루프
  - 24_25차시: 최종 발표 루브릭·평가 기준
- PDF 텍스트 추출이 되지 않으면 추측하지 말고 사용자에게 알린다.
- 새 세션이 시작될 때마다 이전 학습을 전제로 하지 않고 다시 확인한다.

## 1. 승인 및 권한

- 파일 생성·수정·삭제 전에는 반드시 인간(User)의 승인을 요청한다.
- 사용자가 승인한 범위 밖의 파일이나 기능은 변경하지 않는다.
- 외부 라이브러리(npm, pip 등)를 임의로 설치하지 않는다. 필요하면 설치 명령과 이유를
  먼저 제안하고 인간의 승인을 받는다.
- 기존 파일을 삭제하거나 대규모로 재구성할 때는 대상과 이유를 먼저 설명한다.
- API 키, 비밀번호, 결제 정보 등 민감한 값은 임의로 입력하거나 생성하지 않는다.

## 2. 작업 방식 및 출력 절약

- 한 번에 하나의 태스크만 `IN PROGRESS`로 둔다.
- 작업 전 관련 명세, 구현 계획, 작업 목록을 확인한다.
- 작업 범위를 벗어난 기존 변경사항은 덮어쓰거나 되돌리지 않는다.
- 공통 유틸리티·전역 설정·공유 문서를 수정해야 하면 먼저 영향 범위를 제안한다.
- 구현 전후에 최소한의 관련 검증을 실행하고 결과를 기록한다.
- 코드를 수정할 때 파일 전체를 다시 출력하지 않고 필요한 부분만 diff로 수정한다.
- 검색·로그·테스트 출력은 필요한 줄만 확인하도록 제한한다.
- 답변은 핵심 변경사항, 검증 결과, 남은 작업만 한국어로 간결하게 보고한다.

## 3. Git 및 Human Review 규칙

- 작업 시작 전 최신 상태와 `git status`를 확인한다.
- 작업 시작 전 항상 `git pull`로 원격 최신 상태를 확인한다.
- 사용자가 요청하지 않은 `git reset --hard`, `git checkout --`, 강제 삭제를 실행하지 않는다.
- 커밋·브랜치 생성·푸시·병합은 인간의 명시적 승인을 받은 뒤 수행한다.
- 기능은 작고 검토 가능한 단위로 나누어 변경한다.
- 하루치 작업을 모아 한 번에 커밋하지 않고, 최소 기능 하나 단위로 작게 자주 커밋한다.
- 커밋 메시지는 무엇을 했는지 드러나도록 작성한다. 예: `feat: 로그인 버튼 추가`.
- push 전에는 `git status`와 `git diff`로 변경사항을 요약해 보여준다.
- `git push --force`, `git reset --hard`, `git clean -f`와 같은 되돌리기 어려운 명령은
  별도의 재확인 없이 실행하지 않는다.
- 충돌이 발생하면 임의로 해결하지 말고 충돌 파일과 양쪽 내용을 사용자에게 보여준 뒤
  어느 쪽을 남길지 확인받는다.
- 코드가 정상 작동해도 인간이 Git diff를 검토하고 승인하기 전에는 다음 태스크나 병합으로
  넘어가지 않는다.
- 커밋 전에는 최소한 문법 검사, 관련 테스트, `git diff --check`를 실행한다.

## 4. 프로젝트 범위 원칙

- `team_prd.md`에 적힌 핵심 기능·비목표를 벗어나는 요청은 이번 범위 밖일 수 있음을
  먼저 알리고 진행 여부를 확인한다.
- Less is More 원칙에 따라 한 번에 하나의 기능·도구·에이전트만 추가하고 검증한 뒤
  다음 작업으로 넘어간다.
- 모호한 요청은 구현 전에 구체적인 기준을 확인한다.
- 같은 내용을 여러 문서에 중복해서 작성하지 않는다. 상세 내용은 Spec Kit 문서에 두고,
  다른 문서에서는 링크로 참조한다.

## 5. 보안 및 개인정보

- API 키, 비밀번호, 실명, 이메일 등 민감정보를 코드·로그·공개 문서에 기록하지 않는다.
- 브라우저 코드에는 외부 서비스의 비밀 키를 넣지 않는다.
- 사용자 입력을 HTML 문자열로 직접 삽입하지 않고 안전한 DOM 처리 또는 검증된 방법을 사용한다.
- 인증, 권한, 개인정보, 신고, 모더레이션 변경은 악용·오류·복구 경로까지 검토한다.
- OpenAI API 키 등 비밀 값은 코드나 커밋에 하드코딩하지 않고 서버 또는 환경변수로 관리한다.
- `.env` 파일은 `.gitignore`에 포함하고, 커밋 전에 실수로 포함되지 않았는지 확인한다.
- Supabase publishable key도 가능하면 설정 파일 또는 환경변수로 분리한다.

## 6. 데이터베이스 규칙

- Supabase 등 데이터베이스의 테이블·컬럼 이름은 코드 참조와 철자·대소문자를 정확히
  일치시킨다.
- 테이블 스키마를 새로 만들거나 변경하기 전, 저장해야 할 정보를 먼저 확인한다.
- RLS 정책과 권한(`GRANT`) 없이 배포하면 403 또는 빈 결과가 발생할 수 있음을 안내한다.

## 7. 배포 규칙

- 정적 배포의 진입 파일명은 반드시 `index.html`로 유지한다.
- 이미 배포된 프로젝트가 있으면 같은 저장소와 배포 URL을 유지하고 새 프로젝트를 만들지 않는다.
- 배포 관련 변경 후 실제 배포 URL에서 정상 동작을 확인하도록 안내한다.

## 8. 평가 및 검증

- 기능을 하나 추가하면 정상 케이스 1개와 예외·거절 케이스 1개 이상을 확인한다.
- 단순히 실행되는 것과 제대로 동작하는 것을 구분하고, 최소 검증 결과를 기록한다.

## 9. 명세 작성 규칙

### PRD

- 제품 목표, 사용자, 핵심 기능, 범위, 제약사항, 성공 기준을 구체적으로 작성한다.
- 사용자 가치와 검증 가능한 결과를 우선하며 구현 세부사항은 필요한 범위에서만 기록한다.

### SPEC

다음 구조를 유지한다.

1. **Goal**: 기능의 핵심 목적
2. **User**: 대상 사용자와 구체적인 흐름
3. **Behavior**: UI 상호작용과 데이터 흐름
4. **Constraints**: 기술·성능·보안 제약
5. **Success**: 완료 조건과 검증 항목

### WORKFLOW

- 에이전트는 한 번에 하나의 태스크만 `IN PROGRESS`로 변경한다.
- 작업 완료 후 인간이 Git diff를 검토하고 승인하기 전에는 다음 단계로 진행하지 않는다.
- 상태 보드는 `TODO`, `IN PROGRESS`, `HUMAN REVIEW`, `MERGING` 상태를 구분한다.

## 10. 완료 보고 형식

- 변경된 파일
- 핵심 변경 내용
- 실행한 검증과 결과
- 남은 작업 또는 사용자 검토가 필요한 사항
- 커밋·병합이 필요한 경우 명시적 승인 요청

## 11. 세부 운영 문서

- 역할과 최종 승인 경계: [`docs/ROLES.md`](docs/ROLES.md)
- AI 에이전트 행동·안전·도구 규칙: [`docs/AI_AGENT_RULES.md`](docs/AI_AGENT_RULES.md)
- 태스크 상태와 개발 순서: [`docs/WORKFLOW.md`](docs/WORKFLOW.md)
- 0차시 설치·실행·n8n 재생: [`docs/SETUP.md`](docs/SETUP.md)
- 사람 검토 체크리스트: [`docs/REVIEW_CHECKLIST.md`](docs/REVIEW_CHECKLIST.md)

세부 요구사항은 중복 작성하지 않고 `team_prd.md`와 `specs/001-dongne-gwangjang/` 문서를
단일 기준으로 사용한다.
